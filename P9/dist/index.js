const modalHandlerLogin = (modal) => {
    document.querySelector(modal).classList.remove("hidden");
};
const removeModalLogin = (modal) => {
    console.log(modal);
    document.querySelector(modal).classList.add("hidden");
};
const modalHandlerCart = (modal) => {
    document.querySelector(modal).classList.remove("hidden");
};
const checkoutback = (modal) => {
    console.log(modal);
    document.querySelector(modal).classList.add("hidden");
};
